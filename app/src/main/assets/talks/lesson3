Of course, we also use "xml" files and AndroidManifest.xml file, along with java codes files. "xml" files are files whose names end with ".xml". For example, "main.xml", whereas Java files end with ".java". For example, "MainActivity.java".
